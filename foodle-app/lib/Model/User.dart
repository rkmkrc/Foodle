class User {
  final String imagePath;
  final String userName;
  final String email;
  final String about;
  final bool isDarkMode;

  User(this.imagePath, this.userName, this.email, this.about, this.isDarkMode);
}
